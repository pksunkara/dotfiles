pacman during installation:
	netctl, dhcpcd, base, linux, linux-firmware, amd-ucode, efibootmgr, grub, nvidia, nvidia-settings
run:
	nvidia-xconfig
user to be added:
copy ssh key:
pacman:
	xorg-xinit, xorg-server, xorg-xrandr
	binutils, make, which, pkgconf, fakeroot, gcc, git, sudo, man-db, patch, whois
	alsa-utils
	fish, fzf, neovim, openssh, tmux, htop, jq, openssl
	bat, dua-cli, eza, fd, hexyl, pastel, tokei, hyperfine, zola, tealdeer, ripgrep, bandwhich
	docker, docker-compose, docker-buildx
	alacritty, github-cli, i3-wm, mutt
	noto-fonts, noto-fonts-extra, noto-fonts-emoji, noto-fonts-cjk, ttf-fantasque-sans-mono, otf-firamono-nerd
	pacman-contrib
	nix
	git-lfs, git-delta, git-absorb, radicle-bin
	dive
	rustup, mold
	python, uv, python-black, python-prospector?
	nodejs-lts-iron, npm, pnpm
	go, gopls
	stylua
	protobuf, buf
	kubectl, k9s, helm
	gnome-keyring, perl-authen-sasl, perl-io-socket-ssl
	rofi, polybar
	p7zip, unzip,
	aws-cli, asciinema
	ollama
rust:
	install stable
install paru:
paru:
	ods-bin
	dotsync, publisher, enquirer, plotter, crude
	git-interactive-rebase-tool-bin, git-trim-bin
	lazydocker-bin, docker-slim-bin
	google-chrome, visual-studio-code-bin, github-desktop-bin
	supabase-bin, ngrok, devbox-bin
user to be modified:
	add to sudoers file (ex: pksunkara ALL=(ALL) NOPASSWD: ALL)
	add to docker, nix-users groups (ex: usermod -aG docker pksunkara)
	set fish as default shell (change shell)
systemctl enable:
	sshd, docker, systemd-timesyncd
systemctl --user enable:
	ssh-agent, radicle-node
configs:
	.xinitrc, .Xresources (maybe remove?)
links:
	alacritty, fish, git, i3, mutt, nvim, paru, polybar, rofi, tmux
	vscode/ settings.json, keybindings.json, snippets
nix:
	sudo nix-channel --add https://nixos.org/channels/nixos-24.05 nixpkgs
	sudo nix-channel --update
	sudo chown -R pksunkara:nixbld /nix
npm:
	lerna-update-wizard, svg-term-cli, @withgraphite/graphite-cli
cargo:
	cargo-expand, cargo-llvm-cov, cargo-workspaces, cargo-bloat, cargo-modules, cargo-machete, cargo-outdated
gh-extensions:
	mislav/gh-branch, dlvhdr/gh-dash
TODO:
	plymouth, grub theme, greeter
secrets:
	~/.aws/credentials, ~/.terraform.d/credentials.*, ~/.supabase/access-token, ~/.config/ngrok/ngrok.yml,
	~/.graphite_user_config, ~/.config/user.fish
